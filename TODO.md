# TODO

- [ ] Add `--min-stars` / `--max-stars` filter to only download maps within a star rating range
- [ ] Write a `metadata.json` alongside downloads summarizing all fetched maps (hash, name, stars, source, ranked date)
